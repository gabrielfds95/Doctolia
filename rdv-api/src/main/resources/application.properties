# Global configuration
# Nom de l'application
spring.application.name=rdv-api

# Profil actif : dev (H2) par défaut, prod (PostgreSQL) via variable d'env.
# Voir application-dev.properties / application-prod.properties.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Tomcat configuration
# Port 9000 pour eviter le conflit avec le port 8080 par defaut
server.port=9000

# Log level configuration
# Limite les logs des bibliotheques tierces a ERROR
logging.level.root=ERROR

# Log en INFO pour notre propre code
logging.level.com.rdvmedic=INFO

# Voir le port Tomcat au demarrage
logging.level.org.springframework.boot.web.embedded.tomcat=INFO

# JWT Configuration
# En production : définir la variable d'environnement JWT_SECRET
# En dev : la valeur par défaut est utilisée automatiquement
app.jwt.secret=${JWT_SECRET:doctolia-secret-jwt-key-must-be-at-least-32chars-long}
app.jwt.expiration=900000

# MongoDB Configuration (chat) — commune aux deux profils
# Démarrer avec : docker compose up -d
# Le driver Mongo est paresseux (lazy) : l'application démarre même si MongoDB
# n'est pas disponible ; seules les requêtes vers /slots/{id}/messages échoueront.
spring.data.mongodb.host=${MONGO_HOST:localhost}
spring.data.mongodb.port=${MONGO_PORT:27017}
spring.data.mongodb.database=${MONGO_DATABASE:doctolia_chat}
